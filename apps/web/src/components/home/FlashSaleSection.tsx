'use client';
import { useEffect, useState } from 'react'; import Link from 'next/link'; import { Zap } from 'lucide-react'; import apiClient from '@/lib/api-client';
interface FlashSale { id: string; name: string; originalPrice: number; salePrice: number; startTime: string; endTime: string; status: string; }
function remaining(endTime: string){const seconds=Math.max(0,Math.floor((new Date(endTime).getTime()-Date.now())/1000));return{hours:Math.floor(seconds/3600),minutes:Math.floor(seconds%3600/60),seconds:seconds%60}}
export default function FlashSaleSection(){const[sale,setSale]=useState<FlashSale|null>(null);const[timeLeft,setTimeLeft]=useState({hours:0,minutes:0,seconds:0});useEffect(()=>{void apiClient.get<{data?:FlashSale|null}>('/flashsale/current').then(r=>{const value=r.data?.data??null;setSale(value);if(value)setTimeLeft(remaining(value.endTime))}).catch(()=>setSale(null))},[]);useEffect(()=>{if(!sale)return;const timer=setInterval(()=>setTimeLeft(remaining(sale.endTime)),1000);return()=>clearInterval(timer)},[sale]);if(!sale)return null;return <section className="w-full rounded-2xl border border-amber-500/30 bg-amber-500/10 p-6"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><div className="flex items-center gap-3"><div className="rounded-xl bg-amber-500 p-3 text-white"><Zap className="h-6 w-6"/></div><div><h2 className="text-2xl font-black">{sale.name}</h2><p className="text-sm text-muted-foreground">Giá ưu đãi {sale.salePrice.toLocaleString('vi-VN')}đ, giá gốc {sale.originalPrice.toLocaleString('vi-VN')}đ</p></div></div><div className="font-mono font-bold text-amber-700">{String(timeLeft.hours).padStart(2,'0')}:{String(timeLeft.minutes).padStart(2,'0')}:{String(timeLeft.seconds).padStart(2,'0')}</div></div><Link href="/books" className="mt-5 inline-block rounded-lg bg-amber-600 px-4 py-2 text-sm font-bold text-white">Xem sách</Link></section>}
